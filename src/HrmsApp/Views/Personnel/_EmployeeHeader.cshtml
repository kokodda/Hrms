@using HrmsApp.Models;
@model EmployeeViewModel
@{
    string photo;
    if (Model.Employee.Photo != null) { photo = "data:image/png;base64," + Convert.ToBase64String(Model.Employee.Photo); }
    else { photo = string.Format("{0}/unknown.png", "/images"); }
}
<div class="container-fluid" style="padding:0px">
    <div class="row">
        <div class="col-sm-1">
            <a href="#" onclick="editPhoto()" data-toggle="modal" data-target="#photoModal"><img class="img" width="80" height="100" src="@photo" /></a>
        </div>
        <div class="col-sm-10">
            <h1>@Model.EmployeeName</h1>
            <h5 style="color:silver;font-weight:bold">
                @Model.PositionName : @Model.OrgUnitName
            </h5>
        </div>
    </div>
</div>
